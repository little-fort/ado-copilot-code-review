{
    "$schema": "https://raw.githubusercontent.com/Microsoft/azure-pipelines-task-lib/master/tasks.schema.json",
    "id": "b8e3f2a1-9c4d-4e5f-8a6b-7c8d9e0f1a2b",
    "name": "CopilotCodeReviewDev",
    "friendlyName": "Copilot Code Review (Dev)",
    "description": "[Development Build] Perform automated code reviews on pull requests using the official GitHub Copilot CLI",
    "helpMarkDown": "[Learn more about this task](https://github.com/little-fort/ado-copilot-code-review)",
    "category": "Utility",
    "author": "LittleFortSoftware",
    "version": {
        "Major": 0,
        "Minor": 3,
        "Patch": 22
    },
    "instanceNameFormat": "Copilot Code Review (Dev)",
    "minimumAgentVersion": "2.182.1",
    "inputs": [
        {
            "name": "useClaudeCode",
            "type": "boolean",
            "label": "Use Claude Code CLI",
            "required": false,
            "defaultValue": false,
            "helpMarkDown": "When enabled, uses Claude Code CLI (Anthropic) instead of GitHub Copilot CLI for the code review. Requires an Anthropic API key."
        },
        {
            "name": "useCustomModelProvider",
            "type": "boolean",
            "label": "Use Custom Model Provider (BYOK)",
            "required": false,
            "defaultValue": false,
            "helpMarkDown": "When enabled, the GitHub Copilot CLI uses a custom model provider (bring your own key) instead of GitHub-hosted models. Requires a provider base URL, provider type, and an explicit 'model'. A GitHub PAT becomes optional in this mode. Cannot be combined with 'Use Claude Code CLI'. [Learn more](https://docs.github.com/en/copilot/how-tos/copilot-cli/customize-copilot/use-byok-models)",
            "visibleRule": "useClaudeCode != true"
        },
        {
            "name": "githubPat",
            "type": "string",
            "label": "GitHub PAT",
            "required": false,
            "isSecret": true,
            "helpMarkDown": "Personal Access Token for GitHub with Copilot access. Required when using the GitHub Copilot CLI with GitHub-hosted models (default). Optional when 'Use Custom Model Provider' is enabled—providing it additionally enables GitHub-backed features such as code search. Not needed when 'Use Claude Code CLI' is enabled.",
            "visibleRule": "useClaudeCode != true"
        },
        {
            "name": "githubHost",
            "type": "string",
            "label": "GitHub Enterprise Host",
            "required": false,
            "defaultValue": "",
            "helpMarkDown": "Optional. GitHub Enterprise hostname (e.g., 'subdomain.ghe.com') for accounts with data residency. Sets the GH_HOST environment variable so the Copilot CLI validates the GitHub PAT against your enterprise instance instead of github.com. Leave empty for github.com accounts.",
            "visibleRule": "useClaudeCode != true"
        },
        {
            "name": "copilotProviderBaseUrl",
            "type": "string",
            "label": "Model Provider Base URL",
            "required": false,
            "defaultValue": "",
            "helpMarkDown": "Base URL of the custom model provider's API endpoint (e.g., 'https://api.openai.com/v1' or a local Ollama endpoint). Required when 'Use Custom Model Provider' is enabled.",
            "visibleRule": "useCustomModelProvider = true && useClaudeCode != true"
        },
        {
            "name": "copilotProviderType",
            "type": "string",
            "label": "Model Provider Type",
            "required": false,
            "defaultValue": "",
            "helpMarkDown": "Type of the custom model provider. Documented values: 'openai' (any OpenAI-compatible endpoint, including Ollama and vLLM), 'azure', or 'anthropic'. Required when 'Use Custom Model Provider' is enabled.",
            "visibleRule": "useCustomModelProvider = true && useClaudeCode != true"
        },
        {
            "name": "copilotProviderApiKey",
            "type": "string",
            "label": "Model Provider API Key",
            "required": false,
            "isSecret": true,
            "defaultValue": "",
            "helpMarkDown": "API key for authenticating with the custom model provider. Optional—omit for providers that do not require authentication, such as a local Ollama instance.",
            "visibleRule": "useCustomModelProvider = true && useClaudeCode != true"
        },
        {
            "name": "anthropicApiKey",
            "type": "string",
            "label": "Anthropic API Key",
            "required": false,
            "isSecret": true,
            "helpMarkDown": "API key for Anthropic's Claude Code CLI, billed against Anthropic API usage. When 'Use Claude Code CLI' is enabled, provide either this or a Claude Code OAuth Token — not both.",
            "visibleRule": "useClaudeCode = true"
        },
        {
            "name": "claudeCodeOAuthToken",
            "type": "string",
            "label": "Claude Code OAuth Token",
            "required": false,
            "isSecret": true,
            "helpMarkDown": "Long-lived OAuth token generated by running 'claude setup-token' locally, billed against a Claude subscription (Pro/Max/Team/Enterprise) instead of API usage. When 'Use Claude Code CLI' is enabled, provide either this or an Anthropic API Key — not both.",
            "visibleRule": "useClaudeCode = true"
        },
        {
            "name": "maxTurns",
            "type": "string",
            "label": "Max Turns (Claude Code)",
            "required": false,
            "defaultValue": "",
            "helpMarkDown": "Optional. Maximum number of agentic turns for Claude Code CLI. Limits how many tool-use iterations the agent can perform.",
            "visibleRule": "useClaudeCode = true"
        },
        {
            "name": "maxBudget",
            "type": "string",
            "label": "Max Budget USD (Claude Code)",
            "required": false,
            "defaultValue": "",
            "helpMarkDown": "Optional. Maximum cost in USD for the Claude Code CLI session. The session will stop if this budget is reached.",
            "visibleRule": "useClaudeCode = true"
        },
        {
            "name": "azureDevOpsPat",
            "type": "string",
            "label": "Azure DevOps PAT",
            "required": false,
            "helpMarkDown": "Personal Access Token for Azure DevOps. Used for retrieving pull request details and posting review comments. **Required for Azure DevOps Server (on-prem)**. For Azure DevOps Services (cloud), you can use the System Access Token instead by enabling the option below."
        },
        {
            "name": "useSystemAccessToken",
            "type": "boolean",
            "label": "Use System Access Token",
            "required": false,
            "defaultValue": false,
            "helpMarkDown": "When enabled, uses the pipeline's System.AccessToken (OAuth) instead of a PAT. This is the Microsoft-recommended authentication method for Azure DevOps Services. **Note**: The Build Service identity must have 'Contribute to pull requests' permission on the repository. Not supported for Azure DevOps Server (on-prem)."
        },
        {
            "name": "organization",
            "type": "string",
            "label": "Organization",
            "required": false,
            "defaultValue": "",
            "helpMarkDown": "The Azure DevOps organization name for cloud-hosted instances (e.g., 'myorg' from https://dev.azure.com/myorg). For Azure DevOps Server (on-prem), use 'collectionUri' instead. If neither is specified, auto-detected from the pipeline context."
        },
        {
            "name": "collectionUri",
            "type": "string",
            "label": "Collection URI",
            "required": false,
            "defaultValue": "",
            "helpMarkDown": "The full Azure DevOps collection URI (e.g., 'https://dev.azure.com/myorg', 'https://tfs.contoso.com/tfs/DefaultCollection'). If not specified, will be automatically detected from System.CollectionUri. Use this for Azure DevOps Server (on-prem) installations."
        },
        {
            "name": "project",
            "type": "string",
            "label": "Project",
            "required": false,
            "defaultValue": "$(System.TeamProject)",
            "helpMarkDown": "The Azure DevOps project name. Defaults to $(System.TeamProject)."
        },
        {
            "name": "repository",
            "type": "string",
            "label": "Repository",
            "required": false,
            "defaultValue": "$(Build.Repository.Name)",
            "helpMarkDown": "The repository name where the pull request exists. Defaults to $(Build.Repository.Name)."
        },
        {
            "name": "pullRequestId",
            "type": "string",
            "label": "Pull Request ID",
            "required": false,
            "defaultValue": "",
            "helpMarkDown": "The ID of the pull request to review. If not specified, defaults to $(System.PullRequest.PullRequestId) when run as part of a PR validation build."
        },
        {
            "name": "timeout",
            "type": "string",
            "label": "Timeout (minutes)",
            "required": false,
            "defaultValue": "15",
            "helpMarkDown": "The maximum number of minutes to allow the Copilot review to run before timing out. Default is 15 minutes."
        },
        {
            "name": "model",
            "type": "string",
            "label": "Copilot Model",
            "required": false,
            "defaultValue": "",
            "helpMarkDown": "Optional. The Copilot model to use for the review (e.g., 'gpt-4', 'claude-sonnet'). If not specified, uses the default model. **Required** when 'Use Custom Model Provider' is enabled—set it to a model identifier your provider serves."
        },
        {
            "name": "reasoningEffort",
            "type": "string",
            "label": "Reasoning Effort",
            "required": false,
            "defaultValue": "",
            "helpMarkDown": "Optional. Reasoning effort level to control review depth and token spend. GitHub Copilot CLI accepts: none, minimal, low, medium, high, xhigh, max. Claude Code CLI accepts: low, medium, high, xhigh, max. If the selected model does not support the requested level, the CLI fails with a clear error rather than ignoring it. Leave empty to use the model's default."
        },
        {
            "name": "promptFile",
            "type": "filePath",
            "label": "Custom Prompt File",
            "required": false,
            "defaultValue": "",
            "helpMarkDown": "Optional. Path to a .txt file containing a custom prompt for Copilot. The file must exist and contain content."
        },
        {
            "name": "prompt",
            "type": "multiLine",
            "label": "Custom Prompt",
            "required": false,
            "defaultValue": "",
            "helpMarkDown": "Optional. A custom prompt to pass directly to Copilot. If both prompt and promptFile are specified, prompt takes precedence.",
            "properties": {
                "rows": "10",
                "maxLength": "5000"
            }
        },
        {
            "name": "promptRaw",
            "type": "multiLine",
            "label": "Raw Prompt",
            "required": false,
            "defaultValue": "",
            "helpMarkDown": "Optional. A raw prompt to pass directly to the Copilot CLI with no modification. Cannot be combined with other prompt inputs (prompt, promptFile, promptFileRaw).",
            "properties": {
                "rows": "10",
                "maxLength": "5000"
            }
        },
        {
            "name": "promptFileRaw",
            "type": "filePath",
            "label": "Raw Prompt File",
            "required": false,
            "defaultValue": "",
            "helpMarkDown": "Optional. Path to a file containing a raw prompt to pass directly to the Copilot CLI with no modification. Cannot be combined with other prompt inputs (prompt, promptFile, promptRaw)."
        },
        {
            "name": "authors",
            "type": "string",
            "label": "Author Filter",
            "required": false,
            "defaultValue": "",
            "helpMarkDown": "Optional. A comma-separated list of email addresses. If specified, the task will only run code reviews for PRs authored by users with matching email addresses. Uses $(Build.RequestedForEmail) for comparison."
        },
        {
            "name": "includeWorkItems",
            "type": "boolean",
            "label": "Include Work Item Details",
            "required": false,
            "defaultValue": true,
            "helpMarkDown": "When enabled, fetches full details (title, description, acceptance criteria, repro steps) for work items linked to the pull request and includes them as context for the Copilot review. Disable to reduce API calls and context window usage."
        },
        {
            "name": "resolveThreads",
            "type": "pickList",
            "label": "Thread Resolution Scope",
            "required": false,
            "defaultValue": "agentOnly",
            "options": {
                "agentOnly": "Agent threads only",
                "all": "All addressed threads (including human reviewers')"
            },
            "helpMarkDown": "Controls which existing comment threads the review agent may resolve when their issues have been addressed. 'agentOnly' (default): only threads created by previous automated reviews. 'all': additionally allows resolving threads created by human reviewers. Not applied to raw prompt modes (promptRaw/promptFileRaw)."
        },
        {
            "name": "suppressPositiveFeedback",
            "type": "boolean",
            "label": "Suppress Positive Feedback",
            "required": false,
            "defaultValue": false,
            "helpMarkDown": "When enabled, the review agent will not post praise or 'looks good' comments. If a review finds no impactful issues, no comment is posted at all. Not applied to raw prompt modes (promptRaw/promptFileRaw)."
        },
        {
            "name": "maxMinorIssues",
            "type": "string",
            "label": "Max Minor Issues Per Review",
            "required": false,
            "defaultValue": "5",
            "helpMarkDown": "Maximum number of Minor-severity findings included in the consolidated minor-suggestions comment each review pass. Set to 0 to suppress Minor findings entirely. Critical and Major findings are never capped. Not applied to raw prompt modes (promptRaw/promptFileRaw)."
        },
        {
            "name": "diffOnlyReview",
            "type": "boolean",
            "label": "Diff-Only Review",
            "required": false,
            "defaultValue": false,
            "helpMarkDown": "When enabled, the review is restricted to ONLY the PR diff. The diff is pre-computed and embedded in the prompt, and CLI tool access is limited to posting comments only. This dramatically reduces token usage for large repositories. Compatible with all prompt modes — when using raw prompts, the diff context is appended to the end of the prompt."
        },
        {
            "name": "publishPromptArtifacts",
            "type": "boolean",
            "label": "Publish Prompt Artifacts",
            "required": false,
            "defaultValue": false,
            "helpMarkDown": "When enabled, publishes all context files (PR details, iteration details, work items, commit SHAs) and the final assembled prompt as pipeline artifacts. Useful for debugging prompt construction and reviewing what context the CLI agent received."
        }
    ],
    "execution": {
        "Node20_1": {
            "target": "index.js"
        },
        "Node16": {
            "target": "index.js"
        }
    },
    "restrictions": {
        "commands": {
            "mode": "any"
        },
        "settableVariables": {
            "allowed": []
        }
    }
}
